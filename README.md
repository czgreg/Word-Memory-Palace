# 单词记忆宫殿 🏰

> 基于「空间记忆法」的英语单词学习应用 —— 把单词装进你的记忆宫殿

## Goal and Philosophy

将抽象的单词记忆转化为空间化、故事化的学习体验。通过「房子 → 房间 → 单词」的层级结构，结合 AI 生成的场景故事和多维度练习，让背单词不再枯燥。

> 每一个房间都是一个场景，每一个场景都是一段故事，每一段故事都连接着你要记住的单词。

## What's 单词记忆宫殿

一个全栈 Web 应用，核心功能包括：

- 🏠 **房子与房间管理** — 自由组织你的词汇空间
- 📖 **批量录入单词** — 支持 `word: 释义` 格式一次导入，自动补全音标和词性
- 🤖 **AI 故事生成** — 根据房间单词自动生成记忆故事，支持 OpenAI / 通义千问 / DeepSeek / Ollama 本地模型
- 📚 **沉浸式学习** — 阅读带有单词高亮和词性色彩标注的故事
- ✏️ **造句练习** — AI 实时点评你的造句，纠错并给出建议
- 🏆 **挑战模式** — 限时考验，检验记忆效果
- 🎯 **自定义挑战** — 跨房间组合单词进行挑战

## Why 单词记忆宫殿

传统背单词 App 的问题：

1. **机械重复** — 反复刷卡片，缺乏深度加工
2. **孤立记忆** — 每个单词是孤岛，没有语境连接
3. **数据割裂** — 换个浏览器数据就没了

本项目的解决方案：

- ✅ 空间 + 故事双重编码，利用大脑的空间记忆优势
- ✅ AI 将同组单词编织成连贯故事，建立语义网络
- ✅ 本地 SQLite 后端，所有浏览器共享同一份数据

## Requirements

- **Node.js** >= 18
- **npm** >= 9

可选（用于 AI 功能）：
- OpenAI / 通义千问 / DeepSeek 的 API Key，或
- 本地安装 [Ollama](https://ollama.ai/)

## Installation

```bash
# 克隆项目
git clone <repo-url>
cd EnglishLearning

# 安装依赖
npm install
```

## Usage

```bash
# 一键启动（后端 + 前端同时运行）
npm run dev
```

启动后：
- 🌐 前端：`http://localhost:5173`
- 🔧 后端：`http://localhost:3001`
- 💾 数据库：`data/memory-palace.db`（自动创建）

## How It Works

### 技术架构

```
┌─────────────────────────────────────────┐
│              前端 (React + Vite)          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 房间管理  │ │  学习页   │ │  挑战页   │ │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ │
│       └────────────┼────────────┘       │
│              ┌─────┴──────┐              │
│              │ Repository  │              │
│              │   Layer     │              │
│              └─────┬──────┘              │
│              ┌─────┴──────┐              │
│              │ database.js │  ← HTTP 客户端│
│              └─────┬──────┘              │
└────────────────────┬────────────────────┘
                     │ fetch /api/*
┌────────────────────┴────────────────────┐
│          后端 (Express.js :3001)          │
│  ┌──────────────────────────────────┐   │
│  │  /api/db/query  → SELECT         │   │
│  │  /api/db/run    → INSERT/UPDATE   │   │
│  │  /api/db/batch  → 事务批量操作     │   │
│  └───────────────┬──────────────────┘   │
│           ┌──────┴───────┐               │
│           │ better-sqlite3│               │
│           └──────┬───────┘               │
└──────────────────┬──────────────────────┘
             ┌─────┴──────┐
             │ SQLite 文件  │
             │ memory-     │
             │ palace.db   │
             └────────────┘
```

### 项目结构

```
EnglishLearning/
├── server/
│   └── server.js            # Express 后端服务
├── src/
│   ├── components/          # React 组件
│   ├── db/
│   │   ├── database.js      # 数据库 HTTP 客户端
│   │   ├── migrations.js    # 表结构定义
│   │   └── repositories/    # 数据访问层
│   │       ├── houseRepository.js
│   │       ├── roomRepository.js
│   │       ├── wordRepository.js
│   │       └── storyRepository.js
│   ├── hooks/               # React Hooks
│   ├── pages/               # 页面组件
│   │   ├── HomePage.jsx     # 首页仪表盘
│   │   ├── RoomsPage.jsx    # 房间管理
│   │   ├── RoomDetailPage.jsx  # 单词录入
│   │   ├── LearnPage.jsx    # 故事学习
│   │   ├── SentencePracticePage.jsx  # 造句练习
│   │   ├── ChallengePage.jsx        # 挑战模式
│   │   └── CustomChallengePage.jsx   # 自定义挑战
│   └── utils/
│       └── aiService.js     # AI 服务封装
├── data/                    # SQLite 数据库（gitignore）
├── package.json
└── vite.config.js
```

## More

- [记忆宫殿法 - 维基百科](https://zh.wikipedia.org/wiki/%E8%AE%B0%E5%BF%86%E5%AE%AB%E6%AE%BF)
- [Vite 官方文档](https://vite.dev/)
- [better-sqlite3](https://github.com/WiseLibs/better-sqlite3) — 高性能 Node.js SQLite 绑定
- [Ollama](https://ollama.ai/) — 本地运行大语言模型

## License

MIT
